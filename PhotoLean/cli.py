# The entry point for interacting with PhotoLean from the command line.
# Handles argument parsing and routing commands to the appropriate modules
# (e.g., config updates, running optimizations, checking status).
